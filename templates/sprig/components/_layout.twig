<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />

    <title>{{ title }}</title>

    <link rel="stylesheet" href="/src/css/sprig-components.css?v=20210503">
</head>
<body class="bg-cool-gray-200">

    <div class="max-w-5xl mx-auto my-10">
        <div class="flex flex-col space-y-8">
            <div class="flex flex-col space-y-4 text-center">
                <h3 class="text-3xl leading-8 font-extrabold tracking-tight text-gray-900 sm:text-4xl sm:leading-10">
                    <a href="/sprig/components/{{ craft.app.request.segment(3) }}">{{ title }}</a>
                </h3>
                <p class="text-xl leading-7 text-gray-600">
                    {{ subtitle }}
                </p>
            </div>

            {% block content %}{% endblock %}

        </div>
    </div>

    {{ sprig.script }}
</body>
</html>
